import 'package:flutter/material.dart';


class SearchingPageView extends StatelessWidget {
  const SearchingPageView({super.key});

  @override
  Widget build(BuildContext context){
    return Column(
      mainAxisAlignment: MainAxisAlignment.spaceEvenly,
      children: [
        Text(
          "This is Searching Page View",
        ),
      ],
    );
  }
}